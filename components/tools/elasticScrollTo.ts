// utils/elasticScrollTo.ts

/**
 * Scroll animado elástico a un elemento por id
 * @param id string id del elemento destino
 * @param offset number offset opcional en px
 * @param duration number duración en ms
 */
export function elasticScrollToId(id: string, duration = 1200) {
    const target = document.getElementById(id);
    if (!target) return;

}
